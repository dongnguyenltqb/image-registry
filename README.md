# image-registry

1. To generate account for registry
    ```shell
    htpasswd -c .htpasswd username
    ```
2. Run
    ```shell
    docker-compose up -d
    ```
3. UI
    - visit `http://localhost:5009`
